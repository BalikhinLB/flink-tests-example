# flink-tests-example
Project with examples of flink jobs with DI and without DI

Пример простейшего потока, который берет сообщение из kafka (json),
фильтрует их по какому-то признаку и отправляет во внешнюю систему.
com.lb.flink.simple.job
